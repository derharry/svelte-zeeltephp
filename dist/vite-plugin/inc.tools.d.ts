/**
 * uprints msg to console.log and sets the curser to start of last line to overwrite.
 * use console.log to overwrite this and start a new line.
 * @param {*} msg
 */
export function console_log_sameline(msg: any): void;
/**
 * copy a folder
 * @param {string} src
 * @param {string} dest
 * @param {callback_function} filter
 */
export function copyRecursiveSync(src: string, dest: string, filter?: callback_function): void;
